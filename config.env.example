PORT=3000
HOST=0.0.0.0
COOKIE_SECRET=change-this-with-at-least-32-chars
# Optional, aber empfohlen: einmalig setzen und danach unverändert lassen.
PASSWORD_PEPPER=optional-extra-secret
# Optional: 64 Hex-Zeichen für AES-256-GCM-Verschlüsselung von Artikeln.
# Wenn leer, ist die Checkbox "verschlüsseln" im Editor deaktiviert.
# CONTENT_ENCRYPTION_KEY=<64_hex_random>
# 64 Hex-Zeichen für Integritätsprüfung (HMAC-SHA256) der Artikeldateien.
# Wird vom Installer automatisch gesetzt und sollte danach unverändert bleiben.
# CONTENT_INTEGRITY_KEY=<64_hex_random>
# 64 Hex-Zeichen oder starkes Secret für verschlüsselte Backups (Admin-Menü + CLI-Skripte).
# Sollte getrennt von CONTENT_ENCRYPTION_KEY geführt werden.
# BACKUP_ENCRYPTION_KEY=<backup_secret>
# Automatische Backups aktivieren (true/false)
BACKUP_AUTO_ENABLED=false
# Intervall für automatische Backups (in Stunden)
BACKUP_AUTO_INTERVAL_HOURS=24
# Retention: max. Anzahl Backup-Dateien (0 = unbegrenzt)
BACKUP_RETENTION_MAX_FILES=30
# Retention: max. Alter in Tagen (0 = deaktiviert)
BACKUP_RETENTION_MAX_AGE_DAYS=0
SESSION_TTL_HOURS=12
# Versionshistorie: Anzahl aufbewahrter Versionen pro Artikel
VERSION_HISTORY_RETENTION=150
# Versionshistorie: ab dieser Position (neueste=0) werden Versionen als .json.gz komprimiert
# 0 = alle Versionen komprimieren (auch die neueste), hoher Wert = selten/keine Kompression
VERSION_HISTORY_COMPRESS_AFTER=30
WIKI_TITLE=FlatWiki
# Öffentliche Basis-URL für robots/sitemap (inkl. Schema), z.B. https://wiki.example.com
PUBLIC_BASE_URL=
# Optional: "flat" (Standard) oder "sqlite" (Hybrid-Index)
INDEX_BACKEND=flat
BOOTSTRAP_ADMIN_USERNAME=admin
# Optional für Headless-Setup. Ohne diesen Wert erfolgt die Ersteinrichtung visuell über /setup.
# Wird nur beim Erststart verwendet, wenn data/users.json leer ist.
# BOOTSTRAP_ADMIN_PASSWORD=SetARealPassword123
